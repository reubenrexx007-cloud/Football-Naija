{
  "name": "naija-footy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.3.4",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}: src/app/page.tsx
'use client'
return (
    <div className="min-h-screen bg-green-50">
      {/* Header */}
      <header className="bg-green-600 text-white p-4">
        <h1 className="text-2xl font-bold text-center">🇳🇬 NaijaFooty</h1>
        <p className="text-center text-green-100">Your Home for Nigerian Football</p>
      </header>

      {/* Tab Navigation */}
      <nav className="bg-white border-b border-green-200">
        <div className="flex">
import { useState } from 'react'

export default function NaijaFooty() {
  const [activeTab, setActiveTab] = useState('scores')

  const matches = [
    { home: 'Super Eagles', away: 'Ghana Black Stars', score: '2-1', status: 'FT' },
    { home: 'Rivers United', away: 'Enyimba', score: '1-0', status: 'Live 67′' },
    { home: 'Kano Pillars', away: 'Plateau United', score: 'vs', status: '20:00' }
  ]
]tab.id

  const banter = [
    '🦅 Osimhen na our pride o!',
    '⚽ Arsenal fans dey cry since morning 😂',
    '🇳🇬 Green Eagles forever!',
    '💚 Who wan bet say we go win AFCON?'
  ]green-100'
              }`}
            >
              {tab.label}
            </button>
          ))}
        </div>
      </nav>

      {/* Content */}
      <main className="p-4">
        {activeTab === 'scores' && (
          <div className="space-y-4">
            <h2 className="text-xl font-bold text-green-800">Live Scores & Fixtures</h2>
            {matches.map((match, index) => (
              <div key={index} className="bg-white p-4 rounded-lg shadow border-l-4 border-green-500">
                <div className="flex justify-between items-center">
                  <div className="flex-1">
                    <div className="font-semibold">{match.home}</div>
                    <div className="text-gray-600">vs</div>
                    <div className="font-semibold">{match.away}</div>
                  </div>
                  <div className="text-center">
                    <div className="text-2xl font-bold text-green-600">{match.score}</div>
                    <div className={`text-sm px-2 py-1 rounded ${
                      match.status === 'Live 67′' 
                        ? 'bg-red-100 text-red-600' 
                        : 'bg-gray-100 text-gray-600'
                    }`}>
                      {match.status}
                    </div>

  return (React.ReactNode

   '
                    }`}>
                      {match.status}
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        )}

        {activeTab === 'news' && (
          <div className="space-y-4">
            <h2 className="text-xl font-bold text-green-800">Latest Football News</h2>
            {news.map((item, index) => (
              <div key={index} className="bg-white p-4 rounded-lg shadow hover:shadow-md transition-shadow">
                <p className="text-gray-800">{item}</p>
                <div className="mt-2 text-sm text-green-600">2 hours ago • NaijaFooty</div>
              </div>
            ))}
          </div>
        )}

        {activeTab === 'banter' && (
          <div className="space-y-4">
            <h2 className="text-xl font-bold text-green-800">Banter Zone 🔥</h2>
            <p className="text-green-700 italic">Where Nigerian football fans gather!</p>
            
            <div className="space-y-3">
              {banter.map((message, index) => (
                <div key={index} className="bg-white p-4 rounded-lg shadow">
                  <div className="flex items-start space-x-3">
                    <div className="w-8 h-8 bg-green-500 rounded-full flex items-center justify-center text-white font-bold">
                      {String.fromCharCode(65 + index)}
                    </div>
                    <div className="flex-1">
                      <p className="text-gray-800">{message}</p>
                      <div className="mt-1 text-sm text-gray-500">Just now</div>
                    </div>
                  </div>
                </div>
              ))}
            </div>

            <div className="mt-6 bg-green-100 p-4 rounded-lg">
              <textarea 
                placeholder="Drop your banter here... (e.g., 'Wetin dey happen for pitch today?')"
                className="w-full p-3 border border-green-300 rounded-lg resize-none"
                rows={3}
              />
              <button className="mt-2 bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700">
                Post Banter 🚀
              </button>
            </div>
          </div>
        )}
      </main>

      {/* Footer */}
      <footer className="bg-green-800 text-white p-4 mt-8">
        <div className="text-center">
          <p>🇳🇬 Made with 💚 for Nigerian Football Fans</p>
          <p className="text-green-300 text-sm">NaijaFooty - Where passion meets the beautiful game</p>
        </div>
      </footer>
    </div>
  )
}
File 4: src/app/layout.tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'NaijaFooty - Nigerian Football App',
  description: 'Your home for Nigerian football news, scores, and banter',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
File 5: src/app/globals.css
@tailwind base;
@tailwind components;
@tailwind utilities;


* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

html,
body {
  max-width: 100vw;
  overflow-x: hidden;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
} <div className="min-h-screen bg-green-50">
      {/* Header */}
      <header className="bg-green-600 text-white p-4">
  const news = [
    'Osimhen scores hat-trick in Serie A! 🔥',
    'Super Eagles qualify for AFCON 2025! 🦅',
    'NPFL season kicks off next month',
    'Victor Boniface called up for national team'
